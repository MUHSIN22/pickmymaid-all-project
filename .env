# ---------- Global ----------
COMPOSE_PROJECT_NAME=myapp

# ---------- Domains (edit for prod) ----------
# dev defaults (work with hosts file or dns)
ADMIN_HOST=admin.localhost
WEB_HOST=app.localhost
API_HOST=api.localhost

# prod example (uncomment + set):
# ADMIN_HOST=admin.pickmymaid.com
# WEB_HOST=pickmymaid.com
# API_HOST=api.pickmymaid.com

# ---------- SSL / ACME ----------
# Email for Let's Encrypt (prod)
ACME_EMAIL=pickmymaid@gmail.com
# Space-separated domains handled by haproxy/certbot (include all)
ACME_DOMAINS="$ADMIN_HOST $WEB_HOST $API_HOST"

# ---------- Ports ----------
HOST_HTTP_PORT=80
HOST_HTTPS_PORT=443

# ---------- Dev tweaks ----------
CHOKIDAR_USEPOLLING=true
